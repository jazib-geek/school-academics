name: Build and deploy API to Plesk (FTP)
# Secrets on THIS repo (SoftelligentSchoolSuitePro-API):
#   FTP_API_REMOTE_DIR — preferred, e.g. sciencebaseschool.com (folder that contains School.API.dll)
#   FTP_REMOTE_DIR     — fallback if FTP_API_REMOTE_DIR is unset
# Use the same path style as the UI repo secret that already deploys correctly (often /yourdomain.com/ after normalization).

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

concurrency:
  group: deploy-api-${{ github.ref }}
  cancel-in-progress: true

env:
  DOTNET_VERSION: "8.0.x"
  API_PROJECT: School.API/School.API.csproj
  PUBLISH_DIR: ./publish

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore ${{ env.API_PROJECT }}

      - name: Build
        run: dotnet build ${{ env.API_PROJECT }} -c Release --no-restore

      - name: Publish (verify)
        run: dotnet publish ${{ env.API_PROJECT }} -c Release --no-build -o ${{ env.PUBLISH_DIR }}

  deploy:
    needs: build
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore and publish
        run: |
          dotnet restore ${{ env.API_PROJECT }}
          dotnet publish ${{ env.API_PROJECT }} -c Release -o ${{ env.PUBLISH_DIR }}

      - name: Verify publish output (API binaries)
        run: |
          set -euo pipefail
          echo "Commit: ${{ github.sha }}"
          echo "Publish dir: ${{ env.PUBLISH_DIR }}"
          test -f "${{ env.PUBLISH_DIR }}/School.API.dll"
          test -f "${{ env.PUBLISH_DIR }}/School.Application.dll"
          test -f "${{ env.PUBLISH_DIR }}/School.Infrastructure.dll"
          ls -la "${{ env.PUBLISH_DIR }}/School."*.dll
          echo "DLL count in publish: $(find "${{ env.PUBLISH_DIR }}" -maxdepth 1 -name '*.dll' | wc -l)"

      - id: ftp-server-dir
        name: Prepare FTP server-dir
        env:
          FTP_API_REMOTE_DIR: ${{ secrets.FTP_API_REMOTE_DIR }}
          FTP_REMOTE_DIR: ${{ secrets.FTP_REMOTE_DIR }}
        run: |
          set -euo pipefail
          dir="${FTP_API_REMOTE_DIR:-${FTP_REMOTE_DIR:-}}"
          dir="${dir// /}"
          if [ -z "$dir" ]; then
            echo "::error::Set repository secret FTP_API_REMOTE_DIR (or FTP_REMOTE_DIR) to the Plesk folder that contains School.API.dll, e.g. sciencebaseschool.com"
            exit 1
          fi
          # Same normalization as SoftelligentSchoolSuitePro-UI deploy (known working on this host).
          case "$dir" in /*) ;; *) dir="/$dir" ;; esac
          case "$dir" in */) ;; *) dir="${dir}/" ;; esac
          inner="${dir#/}"
          inner="${inner%/}"
          if [ -z "$inner" ]; then
            echo "::error::FTP path resolves to account root ($dir). Use your domain folder, e.g. sciencebaseschool.com"
            exit 1
          fi
          echo "Using server-dir: $dir"
          echo "server_dir=$dir" >> "$GITHUB_OUTPUT"

      - id: ftp-config
        name: Prepare FTP options
        env:
          FTP_PROTOCOL: ${{ secrets.FTP_PROTOCOL }}
        run: |
          protocol="${FTP_PROTOCOL:-ftp}"
          echo "protocol=$protocol" >> "$GITHUB_OUTPUT"

      - name: Upload publish output to Plesk via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.6
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ${{ steps.ftp-config.outputs.protocol }}
          port: 21
          security: loose
          timeout: 300000
          local-dir: ${{ env.PUBLISH_DIR }}/
          server-dir: ${{ steps.ftp-server-dir.outputs.server_dir }}
          dangerous-clean-slate: false
          log-level: verbose
          exclude: |
            **/.git*
            **/.git*/**
            **/appsettings.json
            **/appsettings.*.json
